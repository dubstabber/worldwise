# eslint-plugin-react (<=7.37.5) still declares its peer as eslint "^9.7",
# so npm rejects eslint 10 with ERESOLVE. The plugin works fine with
# eslint 10 at runtime; this flag lets install/ci proceed.
# Remove this once eslint-plugin-react ships eslint 10 peer support.
legacy-peer-deps=true
